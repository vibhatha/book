sudo: required

services:
  -docker
  
language: python

before_install:
  - echo "=============================="
  - echo "Before Install"
  - echo "=============================="
  - python --version


script:
  - echo "=============================="
  - echo "Scripts"
  - echo "=============================="
  - uname -a
  - docker pull laszewski/cloudmesh-tex:0.0.1
  - docker run laszewski/cloudmesh-tex:0.0.1 
  - docker images
  - docker ps -a
  - docker images ls
  - ls
  - python --version

